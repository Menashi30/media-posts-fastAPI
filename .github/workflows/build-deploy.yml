name: Build and Deploy Code


on: [push,pull_requests]

jobs:
  job1:
   runs-on: ubuntu-latest
  steps :
    - name: pulling git repo
      uses: actions/checkout@v5
    - name: say hi to Meena
      run: echo "hi meena"